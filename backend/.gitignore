# Engine runtime output (task index, generated videos) — written cwd-relative
# by PersistenceService(output_dir="output") when scripts run from backend/.
output/

# Local object-storage backend root (generated video artifacts).
.local-storage/
